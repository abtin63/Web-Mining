import nltk
from nltk.book import *


def frequ(text):
    content = [w for w in text if w.isalpha()]
    fdist = FreqDist(content)
    keys = fdist.keys()
    return keys[:30]

print frequ(text1)
print frequ(text5)
